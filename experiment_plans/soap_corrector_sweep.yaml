# Collar corrector: u = u_soft + chi*c. 3 PDEs x frozen/unfrozen x three
# collar geometries (sigma 0.03, sigma 0.2, adaptive q0.9), 30k epochs each.
# NINE ACTIVE RUNS (unfrozen only); the frozen nine are written out but
# commented at the bottom. Root and phase 3 are both SKIPPED -- every
# run loads roots_checkpoints/single_precision/adaptive_sampling/<pde>_experts.pt
# and trains only the composition.
#
# WHY. Two campaigns of ordinary joint fine-tuning have now failed to beat the
# phase-3 checkpoint on any PDE at any collar width. In the last one, six of
# nine runs had their best at EPOCH 1 -- the fine-tune never improved on its own
# untrained starting point. Measured on these checkpoints at sigma 0.03, the
# collar holds 99.90% (burgers), 99.80% (allen_cahn) and 99.94% (schrodinger)
# of sum(r^2) while covering only ~6-7% of the area, with mean r^2 inside/outside
# of 12916x / 8190x / 27409x. The composed residual is not measuring the
# solution; it is measuring the blend.
#
# The corrector attacks that directly. chi = 1 - sum(psi~_j^2) is EXACTLY zero
# wherever a single leaf owns the point, so c can only alter the composition in
# the overlaps. With corrector_init: zero, c == 0 at construction, so the run
# starts exactly at u_soft.
#
# chi > 0 covers:      sigma=0.03    sigma=0.2    adaptive q0.9
#   burgers1d           6.9%          41.6%        11.7%
#   allen_cahn          6.5%          37.8%        13.1%
#   schrodinger         6.4%          38.5%        16.5%
#
# The adaptive geometry sits between the two fixed ones and is the only one
# that varies per face: the scan widens where the root's directional gradient
# stays high and falls back to the 0.03 floor where the field is quiet
# (measured sigma-equivalents: burgers mean 0.050 max 0.144, allen_cahn mean
# 0.121 max 0.396, schrodinger mean 0.130 max 0.490).
#
# FROZEN (freeze_experts: true) is a free lower bound: only c trains, the
# expert interiors are unreachable, and best-model selection can only keep
# something at least as good as the start. It cannot produce a worse checkpoint
# than doing nothing. corrector_collar_ratio 1.0 -- every residual point goes
# into the collar, because d(u)/d(c) = chi is exactly 0 everywhere else and
# points outside would contribute literally nothing.
#
# UNFROZEN (freeze_experts: false) trains the leaf experts alongside c, so the
# interiors are NOT protected and this variant can regress like the ordinary
# fine-tune. corrector_collar_ratio 0.3 keeps 70% of the residual budget
# uniform over the domain so the interiors stay anchored.
#
# sigma 0.2 gives the corrector ~6x more area to act on, at the cost of a worse
# starting point (soft-blend rel-L2 vs the hard phase-3 value: burgers 46x,
# allen_cahn 4.7x, schrodinger 1.3x). frozen + sigma 0.2 is the most promising
# cell -- most room, interiors protected. unfrozen + sigma 0.2 is the riskiest.
#
# corrector_architecture is null, so c defaults to experts_architecture
# [2,20,20,20,out] = 921 params -- ONE network covering every seam in the
# domain. At sigma 0.03 that means representing a function which is zero
# outside ~6.5% of the area and does something different in each of 6-10
# disconnected thin strips. If the frozen runs underperform, this is the first
# knob to widen.
#
# collar_margin stays 1.0, i.e. no effect: the sampled band is exactly the
# window support (build_collar_info scales delta by margin).
#
# THE FROZEN HALF IS COMMENTED OUT for this pass -- the unfrozen runs go
# first. The frozen blocks are written out below the active ones so they can be
# enabled without rebuilding anything. Note that means this pass has NO free
# lower bound in it: every active run can regress, since freeze_experts: false
# leaves the expert interiors trainable.
#
# Budget (active): 9 x 30k = 270k. Frozen half, when enabled: another 270k.
# fine_tune.min_epochs 10000, patience 10 evals.
#
# This is the DEFAULT plan: `python run_experiments.py` with no argument reads
# experiments_plan.yaml, so AWS_scripts/run_and_terminate.sh launches it as-is.
# A copy is kept at experiment_plans/soap_corrector_sweep.yaml.
#
# Run:  python run_experiments.py
#   or: bash AWS_scripts/run_and_terminate.sh      (runs, then stops the box)
base_config:
  activation: tanh
  adam:
    betas:
    - 0.9
    - 0.999
    eps: 1.0e-08
  soap:
    # Released jaxpi SOAP config (examples/*/configs/*soap.py), which is the
    # artifact behind the published PINN numbers. The paper text instead
    # recommends beta1=0.99 from its ablation -- worth a later sweep.
    betas:
    - 0.9
    - 0.999
    # EMA decay of the Shampoo preconditioner. Upstream's -1 sentinel reuses
    # betas[1], which would silently stretch the preconditioner's memory to
    # 0.999 (~1000 steps) purely as a side effect of the beta2 above. The SOAP
    # paper runs betas (0.95, 0.95), i.e. a 0.95 preconditioner EMA, so pin it
    # there and let beta2 govern Adam's second moment alone.
    shampoo_beta: 0.95
    precondition_frequency: 2
    weight_decay: 0.0
  adaptive_pinn:
    AToE_threshold_capacity: null
    M_experts_num: 8
    blending_mode: soft
    enable_gradient_diagnostics: false
    enable_timing: false
    enabled: true
    expert_type: mlp
    fine_tune:
      corrector_architecture: null
      corrector_init: zero
      single_corrector: false
      # 0 = ROOTS-ONLY campaign. The tree is still built and the leaves are
      # still spawned (free diagnostics), but no expert is trained.
      epochs: 0
      lr: 0.001
      lr_schedule: exponential
      lr_warmup_steps: 5000
      lr_decay_rate: 0.9
      # 5000, not the 2000 used by root/phase-3. The jaxpi schedule is built
      # for a segment that converges early (the paper reports SOAP reaching
      # ~1e-4 within 10k iterations) and then polishes; decay every 2000 over
      # 100k is 47 decays, ending at ~7e-6. The previous run confirmed that is
      # too aggressive HERE: allen_cahn's fine-tune spent its last 60k epochs
      # below 1.7e-4 and was still improving (rel-L2 0.465 -> 0.315) when the
      # budget ran out. 5000 gives 19 decays, ending at ~1.4e-4, and is the
      # value jaxpi's own allen_cahn/configs/soap.py uses.
      lr_decay_steps: 5000
      # Grace period before the plateau test can fire. Covers the 5k-step
      # warmup plus 5k of decay, so a noisy/flat rel-L2 during the LR ramp
      # cannot early-stop the segment. Fine-tune reads this key; root and
      # phase 3 read the top-level min_epochs instead.
      min_epochs: 10000
      optimizer_1: soap
      optimizer_2: null
      optimizer_switch_epoch: 99999
      sigma_fraction: 0.03
      adaptive_collar_size: false
      adaptive_collar_quantile: 0.9
      # 0.03: the floor now matches the width phase 3 was trained against, so
      # the gradient scan can NARROW back to the trained geometry wherever the
      # field is quiet and widen only at the faces that cut a feature. The old
      # 0.1 floor was set when 0.03 was the failing width; on these checkpoints
      # 0.03 is the healthy one (soft rel-L2 at parity with hard on all three
      # PDEs), so forcing every face to at least 0.1 only added cost.
      # Measured, no training: the floor is nearly irrelevant either way --
      # burgers 22.1x hard at 0.03 vs 24.7x at 0.1, schrodinger 16.15x vs
      # 16.50x. Whatever C costs comes from the faces the scan CHOOSES to
      # widen, not from the clamp.
      adaptive_collar_sigma_min: 0.03
      adaptive_collar_sigma_max: 0.5
      adaptive_collar_grid: 257
      # Corrector knobs (single_corrector: true). freeze_experts=false trains
      # the leaf experts jointly with c, so the interiors are NOT protected;
      # corrector_collar_ratio must then stay well below its 1.0 default or the
      # interiors would be starved of residual points and drift.
      freeze_experts: true
      corrector_collar_ratio: 1.0
    initial_train:
      epochs: 100000
      lr: 0.001
      lr_schedule: exponential
      lr_warmup_steps: 5000
      lr_decay_rate: 0.9
      lr_decay_steps: 2000
      optimizer_1: soap
      optimizer_2: null
      optimizer_switch_epoch: 99999
    max_experts: 150
    split_icbc:
      enabled: true
    tree_max_depth: 15
    tree_min_samples_leaf: 50
    variable_for_expert_size: norm
    variable_for_node_accept: norm
  allen_cahn:
    D: 0.0001
    adaptive_sampling:
      adaptive_ratio: 0.5
      # OFF for the corrector campaign. RAD and collar sampling are two ways of
      # doing the same thing here and the collar knob is the exact one: it
      # selects the chi>0 region by geometric mask, where RAD only leans that
      # way with a ~3.5x probability bias. Leaving RAD on would (a) waste ~40%
      # of the budget in the frozen runs, since d(u)/d(c) = chi is EXACTLY zero
      # outside the collar, (b) push the unfrozen runs toward the collar, which
      # carries 99.8-99.9% of sum(r^2) and is what has destroyed the interiors
      # in every previous campaign, and (c) collide with corrector_collar_ratio
      # in the residual budget (adaptive 0.5 + collar 1.0 > 1 trims the collar
      # share and warns on every resample). It would also confound the first
      # corrector run with a second variable.
      enabled: false
      # RAD weighting from Wu et al. (CMAME 2023), the reference the SOAP-PINN
      # paper cites: err = |r|^k / mean(|r|^k) + c, with k = phi_power and
      # c = phi_epsilon. The mean-normalisation makes the weights scale-free
      # as the residual shrinks by orders of magnitude, and the +c floor keeps
      # every region at nonzero probability. The previous 'quadratic' had
      # neither: on a spiky field it spans ~1e10 in probability, so a region
      # that goes quiet is abandoned and never revisited.
      phi: rad
      phi_epsilon: 1.0
      phi_power: 1.0
    causal_training:
      enabled: false
      min_weight_threshold: 0.99
      num_chunks: 32
      tol_schedule:
      - 0.01
      - 0.1
      - 1.0
    expert_grad_clip_norm: null
    fourier_features:
      dim: 10
      enabled: false
      periodic: false
      scale: 3.0
    grad_clip_norm: null
    init:
      hidden: default
      ls_use_bias: false
      output: default
    loss_weights:
      bc: 1.0
      continuity: 0.0
      ic: 1.0
      residual: 1.0
    lra:
      alpha: 0.1
      enabled: false
      epsilon: 1.0e-05
      scheme: grad_norm
      update_every: 100
    new_norm_threshold: 0.04
    output_dim: 1
    # RAD root, rel-L2 1.442e-02 @ ep100000. Root is SKIPPED in both tracks.
    pretrained_base_checkpoint: roots_checkpoints/single_precision/adaptive_sampling/allen_cahn_root.pt
    # Phase-3 experts (7 leaves, 3x20), hard rel-L2 2.057e-02 @ ep100000.
    # Set for EVERY run this campaign, so root and phase 3 are both
    # skipped and pretrained_base_checkpoint above is ignored
    # (the orchestrator logs that) -- the expert checkpoint already
    # carries the base weights.
    pretrained_local_expert_checkpoint: roots_checkpoints/single_precision/adaptive_sampling/allen_cahn_experts.pt
    rwf:
      enabled: false
      mean: 1.0
      std: 0.1
    spatial_dim: 1
    spatial_domain:
    - - -1.0
      - 1.0
    temporal_domain:
    - 0.0
    - 1.0
    time_marching:
      enabled: false
      freeze_previous_windows: true
      m_distribution: quadratic
      num_windows: 5
    tree_smoothness_threshold: 0.5
    wavelet_threshold: 0.002
    window_smoothness_order: 2
    window_type: smoothstep
  batch_size: 99999
  burgers1d:
    adaptive_sampling:
      adaptive_ratio: 0.5
      # OFF for the corrector campaign. RAD and collar sampling are two ways of
      # doing the same thing here and the collar knob is the exact one: it
      # selects the chi>0 region by geometric mask, where RAD only leans that
      # way with a ~3.5x probability bias. Leaving RAD on would (a) waste ~40%
      # of the budget in the frozen runs, since d(u)/d(c) = chi is EXACTLY zero
      # outside the collar, (b) push the unfrozen runs toward the collar, which
      # carries 99.8-99.9% of sum(r^2) and is what has destroyed the interiors
      # in every previous campaign, and (c) collide with corrector_collar_ratio
      # in the residual budget (adaptive 0.5 + collar 1.0 > 1 trims the collar
      # share and warns on every resample). It would also confound the first
      # corrector run with a second variable.
      enabled: false
      # RAD weighting from Wu et al. (CMAME 2023), the reference the SOAP-PINN
      # paper cites: err = |r|^k / mean(|r|^k) + c, with k = phi_power and
      # c = phi_epsilon. The mean-normalisation makes the weights scale-free
      # as the residual shrinks by orders of magnitude, and the +c floor keeps
      # every region at nonzero probability. The previous 'quadratic' had
      # neither: on a spiky field it spans ~1e10 in probability, so a region
      # that goes quiet is abandoned and never revisited.
      phi: rad
      phi_epsilon: 1.0
      phi_power: 1.0
    causal_training:
      enabled: false
      min_weight_threshold: 0.99
      num_chunks: 32
      tol_schedule:
      - 0.01
      - 0.1
      - 1.0
    expert_grad_clip_norm: null
    fourier_features:
      dim: 128
      enabled: false
      periodic: false
      scale: 1.0
    grad_clip_norm: null
    init:
      hidden: default
      ls_use_bias: false
      output: default
    loss_weights:
      bc: 1.0
      continuity: 0.0
      ic: 1.0
      residual: 1.0
    lra:
      alpha: 0.1
      enabled: false
      epsilon: 1.0e-05
      scheme: grad_norm
      update_every: 100
    new_norm_threshold: 0.015
    nu: 0.01
    output_dim: 1
    # RAD root, rel-L2 3.672e-05 @ ep64000. Root is SKIPPED in both tracks.
    pretrained_base_checkpoint: roots_checkpoints/single_precision/adaptive_sampling/burgers_root.pt
    # Phase-3 experts (6 leaves, 3x20), hard rel-L2 5.308e-03 @ ep85000.
    # Set for EVERY run this campaign, so root and phase 3 are both
    # skipped and pretrained_base_checkpoint above is ignored
    # (the orchestrator logs that) -- the expert checkpoint already
    # carries the base weights.
    pretrained_local_expert_checkpoint: roots_checkpoints/single_precision/adaptive_sampling/burgers_experts.pt
    rwf:
      enabled: false
      mean: 1.0
      std: 0.1
    spatial_dim: 1
    spatial_domain:
    - - -1.0
      - 1.0
    temporal_domain:
    - 0.0
    - 1.0
    time_marching:
      enabled: false
      freeze_previous_windows: true
      m_distribution: quadratic
      num_windows: 5
    tree_smoothness_threshold: 0.5
    wavelet_threshold: 0.001
    window_smoothness_order: 2
    window_type: smoothstep
  cuda: true
  epochs: 100000
  eval_every: 1000
  eval_only: false
  lbfgs:
    history_size: 100
    line_search: strong_wolfe
    lr: 1.0
    max_iter: 1
    tolerance_change: 0.0
    tolerance_grad: 0.0
  lr: 0.001
  lr_decay_rate: 0.9
  lr_decay_steps: 2000
  lr_schedule: exponential
  lr_warmup_start_factor: 0.01
  lr_warmup_steps: 5000
  # Applies to the root and phase-3 segments (fine_tune has its own key).
  # 30000, up from 10000. Schrodinger's root is bistable: in the campaign that
  # worked it sat at rel-L2 ~1.0 through epoch 2000 and only broke through at
  # 3000, and in the campaign that failed it never broke through and
  # patience_evals=10 killed it at 11000 with rel-L2 0.92. A 10k floor gives a
  # bistable problem 11 evals to escape, which is a coin flip. Burgers stopped
  # at 35k last time, so this floor is nearly free for the others.
  min_epochs: 30000
  model: AToELeaves
  optimizer_1: soap
  optimizer_2: null
  optimizer_switch_epoch: 99999
  # Stop a segment after 10 consecutive evals (= 10k epochs at eval_every
  # 1000) without a >1% rel-L2 improvement over the patience-local best.
  # The counter resets on any qualifying improvement, so this is "10k epochs
  # since the last real gain", not a wall-clock cap.
  patience_evals: 10
  patience_rel_delta: 0.01
  precision: float32
  print_every: 1000
  # Bounded torch.profiler window per segment, written to
  # <run_dir>/profile_<segment>.txt. Observer only: no RNG or tensor math is
  # touched, so results are bit-identical with it on or off, and any profiler
  # error disables itself and lets training continue. 55 of 100k epochs are
  # recorded, so the cost is seconds. Read the PERCENTAGES in the report --
  # absolute times inside a profiled window are inflated by the profiler.
  profiling:
    # OFF. The previous run's schrodinger job was SIGKILLed (exit -9, the OOM
    # killer) with its last log line being the fine-tune profiler opening its
    # window; the two jobs that survived closed the same window in ~2 minutes.
    # Its fine-tune window is the heaviest of the three (10 experts in a Python
    # forward loop, 80 parameter tensors through SOAP). A SIGKILL cannot be
    # caught, so the fail-safe wrapping in trainer/profiling.py is no help --
    # the only guard is to not open the window. The profiling questions are
    # answered; turn this back on deliberately when there is a new one.
    enabled: false
    segments: all
    start_epoch: 2000
    warmup: 5
    active: 50
    # 100 rather than 30: at 30 the listed rows accounted for only ~22% of
    # Self CPU, so the remainder was invisible in the tail rather than
    # genuinely unattributed. Costs nothing but report length.
    top_k: 100
    record_shapes: false
    emit_trace: false
  problem: burgers1d
  resume_from: null
  sampling:
    collar_data_ratio: 0.0
    collar_margin: 1.0
    eval_train_ratio: 0.25
    # Back ON for the phase-3 track. It was 0 while phase 3 might have used
    # adaptive sampling, where the top-up (uniform, inside the box) refilled
    # exactly the leaves a residual-weighted draw had deliberately starved.
    # That conflict is gone: phase 3 now always samples uniformly
    # (trainer/epoch_loop.py forces it), so it takes _sample_per_expert_direct
    # and the floor is a clean allocation floor,
    #     n_e = max(min_points_per_expert, round(n_res * vol_e / vol_domain)).
    # Without it a thin leaf gets round(8192 * vol_e/vol) points -- for a
    # sliver like allen_cahn's 0.04-wide E3 that is close to zero, and the
    # expert simply stops training. Read only by adaptive/subdomain_data.py,
    # so it is inert in the root and fine-tune segments.
    min_points_per_expert: 1024
    n_boundary_train: 256
    n_initial_train: 256
    n_residual_train: 8192
    plot_samples_every: 5000
    # RAD RE-TARGET period. Under adaptive sampling this is how often the
    # residual DISTRIBUTION is rebuilt; the uniform half of the draw is still
    # refreshed every epoch (jaxpi/SOAP behaviour). With adaptive sampling OFF
    # this key keeps its old meaning and Adam/SOAP still redraw every epoch.
    #
    # 1000 is the Wu et al. value. It was effectively 1 in the last campaign
    # because trainer/epoch_loop.py forced the cadence to 1 for Adam/SOAP --
    # that override is correct for uniform resampling (it is exactly what
    # jaxpi does) but turned RAD into a bootstrap filter re-targeting on a
    # one-step-old residual estimate. That is what broke the roots.
    resample_every_epochs: 1000
    # Fresh uniform candidate pool drawn at each re-target, from which the
    # adaptive half is selected WITHOUT replacement. Wu et al. use 100k. The
    # old path drew from the previous epoch's own 8192 points, so the support
    # fed on itself. Costs one extra forward pass per 1000 epochs.
    rad_pool_size: 100000
    sample_volume_ratio: 35
  save_every: 999999
  schrodinger:
    adaptive_sampling:
      adaptive_ratio: 0.5
      # OFF for the corrector campaign. RAD and collar sampling are two ways of
      # doing the same thing here and the collar knob is the exact one: it
      # selects the chi>0 region by geometric mask, where RAD only leans that
      # way with a ~3.5x probability bias. Leaving RAD on would (a) waste ~40%
      # of the budget in the frozen runs, since d(u)/d(c) = chi is EXACTLY zero
      # outside the collar, (b) push the unfrozen runs toward the collar, which
      # carries 99.8-99.9% of sum(r^2) and is what has destroyed the interiors
      # in every previous campaign, and (c) collide with corrector_collar_ratio
      # in the residual budget (adaptive 0.5 + collar 1.0 > 1 trims the collar
      # share and warns on every resample). It would also confound the first
      # corrector run with a second variable.
      enabled: false
      # RAD weighting from Wu et al. (CMAME 2023), the reference the SOAP-PINN
      # paper cites: err = |r|^k / mean(|r|^k) + c, with k = phi_power and
      # c = phi_epsilon. The mean-normalisation makes the weights scale-free
      # as the residual shrinks by orders of magnitude, and the +c floor keeps
      # every region at nonzero probability. The previous 'quadratic' had
      # neither: on a spiky field it spans ~1e10 in probability, so a region
      # that goes quiet is abandoned and never revisited.
      phi: rad
      phi_epsilon: 1.0
      phi_power: 1.0
    causal_training:
      enabled: false
      min_weight_threshold: 0.99
      num_chunks: 32
      tol_schedule:
      - 0.01
      - 0.1
      - 1.0
    expert_grad_clip_norm: null
    fourier_features:
      dim: 128
      enabled: false
      periodic: false
      scale: 1.0
    grad_clip_norm: null
    init:
      hidden: default
      ls_use_bias: false
      output: default
    loss_weights:
      bc: 1.0
      continuity: 0.0
      ic: 1.0
      residual: 1.0
    lra:
      alpha: 0.1
      enabled: false
      epsilon: 1.0e-05
      scheme: grad_norm
      update_every: 100
    new_norm_threshold: 2.0
    output_dim: 2
    # RAD root, rel-L2 7.732e-04 @ ep100000. Root is SKIPPED in both tracks.
    pretrained_base_checkpoint: roots_checkpoints/single_precision/adaptive_sampling/schrodinger_root.pt
    # Phase-3 experts (10 leaves, 3x20), hard rel-L2 8.065e-04 @ ep79000.
    # Set for EVERY run this campaign, so root and phase 3 are both
    # skipped and pretrained_base_checkpoint above is ignored
    # (the orchestrator logs that) -- the expert checkpoint already
    # carries the base weights.
    pretrained_local_expert_checkpoint: roots_checkpoints/single_precision/adaptive_sampling/schrodinger_experts.pt
    rwf:
      enabled: false
      mean: 1.0
      std: 0.1
    spatial_dim: 1
    spatial_domain:
    - - -5.0
      - 5.0
    temporal_domain:
    - 0.0
    - 1.5707963267948966
    time_marching:
      enabled: false
      freeze_previous_windows: true
      m_distribution: quadratic
      num_windows: 5
    tree_smoothness_threshold: 0.7
    wavelet_threshold: 0.1
    window_smoothness_order: 2
    window_type: smoothstep
  seed: 42
  ssbroyden:
    lr: 1.0
    tolerance_grad: 1.0e-10
experiment_name: soap_corrector_sweep
experiments:
# Root + phase 3 skipped via per-problem pretrained_local_expert_checkpoint.
#
# ACTIVE: unfrozen (freeze_experts: false) -- the leaf experts train
# alongside c, so the interiors are NOT protected and these runs can
# regress. corrector_collar_ratio 0.3 keeps 70% of the residual budget
# uniform over the domain to anchor them.
# ---- unfrozen, collar geometry: s003 ----
- name: burgers1d_unfrozen_s003
  problem: burgers1d
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.03
      adaptive_collar_size: false
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
  experts_architecture: [2, 20, 20, 20, 1]
- name: allen_cahn_unfrozen_s003
  problem: allen_cahn
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.03
      adaptive_collar_size: false
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
  experts_architecture: [2, 20, 20, 20, 1]
- name: schrodinger_unfrozen_s003
  problem: schrodinger
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.03
      adaptive_collar_size: false
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 2]
  experts_architecture: [2, 20, 20, 20, 2]
# ---- unfrozen, collar geometry: s02 ----
- name: burgers1d_unfrozen_s02
  problem: burgers1d
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.2
      adaptive_collar_size: false
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
  experts_architecture: [2, 20, 20, 20, 1]
- name: allen_cahn_unfrozen_s02
  problem: allen_cahn
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.2
      adaptive_collar_size: false
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
  experts_architecture: [2, 20, 20, 20, 1]
- name: schrodinger_unfrozen_s02
  problem: schrodinger
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.2
      adaptive_collar_size: false
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 2]
  experts_architecture: [2, 20, 20, 20, 2]
# ---- unfrozen, collar geometry: adaptive ----
- name: burgers1d_unfrozen_adaptive
  problem: burgers1d
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.03
      adaptive_collar_size: true
      adaptive_collar_quantile: 0.9
      adaptive_collar_sigma_min: 0.03
      adaptive_collar_sigma_max: 0.5
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
  experts_architecture: [2, 20, 20, 20, 1]
- name: allen_cahn_unfrozen_adaptive
  problem: allen_cahn
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.03
      adaptive_collar_size: true
      adaptive_collar_quantile: 0.9
      adaptive_collar_sigma_min: 0.03
      adaptive_collar_sigma_max: 0.5
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
  experts_architecture: [2, 20, 20, 20, 1]
- name: schrodinger_unfrozen_adaptive
  problem: schrodinger
  model: AToELeaves
  epochs: 0
  adaptive_pinn:
    fine_tune:
      epochs: 30000
      single_corrector: true
      freeze_experts: false
      corrector_collar_ratio: 0.3
      corrector_init: zero
      corrector_architecture: null
      sigma_fraction: 0.03
      adaptive_collar_size: true
      adaptive_collar_quantile: 0.9
      adaptive_collar_sigma_min: 0.03
      adaptive_collar_sigma_max: 0.5
  base_architecture: [2, 100, 100, 100, 100, 100, 100, 2]
  experts_architecture: [2, 20, 20, 20, 2]

# ══════════════════════════════════════════════════════════════════
# DISABLED: frozen (freeze_experts: true). Uncomment to run.
#
# Only c trains; the expert interiors are unreachable because chi is
# exactly 0 wherever a single leaf owns the point, and corrector_init:
# zero starts c at 0. With best-model selection these CANNOT produce a
# checkpoint worse than doing nothing -- they are the free lower bound.
# corrector_collar_ratio 1.0: every residual point goes in the collar,
# since d(u)/d(c) = chi is exactly 0 everywhere else.
# ══════════════════════════════════════════════════════════════════
# ---- frozen, collar geometry: s003 ----
# - name: burgers1d_frozen_s003
#   problem: burgers1d
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.03
#       adaptive_collar_size: false
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
#   experts_architecture: [2, 20, 20, 20, 1]
# - name: allen_cahn_frozen_s003
#   problem: allen_cahn
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.03
#       adaptive_collar_size: false
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
#   experts_architecture: [2, 20, 20, 20, 1]
# - name: schrodinger_frozen_s003
#   problem: schrodinger
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.03
#       adaptive_collar_size: false
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 2]
#   experts_architecture: [2, 20, 20, 20, 2]
# ---- frozen, collar geometry: s02 ----
# - name: burgers1d_frozen_s02
#   problem: burgers1d
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.2
#       adaptive_collar_size: false
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
#   experts_architecture: [2, 20, 20, 20, 1]
# - name: allen_cahn_frozen_s02
#   problem: allen_cahn
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.2
#       adaptive_collar_size: false
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
#   experts_architecture: [2, 20, 20, 20, 1]
# - name: schrodinger_frozen_s02
#   problem: schrodinger
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.2
#       adaptive_collar_size: false
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 2]
#   experts_architecture: [2, 20, 20, 20, 2]
# ---- frozen, collar geometry: adaptive ----
# - name: burgers1d_frozen_adaptive
#   problem: burgers1d
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.03
#       adaptive_collar_size: true
#       adaptive_collar_quantile: 0.9
#       adaptive_collar_sigma_min: 0.03
#       adaptive_collar_sigma_max: 0.5
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
#   experts_architecture: [2, 20, 20, 20, 1]
# - name: allen_cahn_frozen_adaptive
#   problem: allen_cahn
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.03
#       adaptive_collar_size: true
#       adaptive_collar_quantile: 0.9
#       adaptive_collar_sigma_min: 0.03
#       adaptive_collar_sigma_max: 0.5
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 1]
#   experts_architecture: [2, 20, 20, 20, 1]
# - name: schrodinger_frozen_adaptive
#   problem: schrodinger
#   model: AToELeaves
#   epochs: 0
#   adaptive_pinn:
#     fine_tune:
#       epochs: 30000
#       single_corrector: true
#       freeze_experts: true
#       corrector_collar_ratio: 1.0
#       corrector_init: zero
#       corrector_architecture: null
#       sigma_fraction: 0.03
#       adaptive_collar_size: true
#       adaptive_collar_quantile: 0.9
#       adaptive_collar_sigma_min: 0.03
#       adaptive_collar_sigma_max: 0.5
#   base_architecture: [2, 100, 100, 100, 100, 100, 100, 2]
#   experts_architecture: [2, 20, 20, 20, 2]
